{% extends 'Layout/base.html.twig' %}

{% block main %}
    {{ form_start(toto) }}
    {{ form_label(toto.title) }}
    {{ form_widget(toto.title) }}
    {{ form_widget(toto.submit, {'attr': {'class': 'btn btn-success'}}) }}
    {{ form_end(toto) }}
{% endblock main %}
