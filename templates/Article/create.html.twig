{% extends 'Layout/base.html.twig' %}

{% block main %}
    {{ form_start(toto) }}
    {{ form_label(toto.title) }}
    {{ form_widget(toto.title, {'attr': {'class': 'salut ca va ?'}}) }}
    {{ form_end(toto) }}
{% endblock main %}
